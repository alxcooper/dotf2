-- Complete Ruby/Lua/etc. blocks using the syntax tree, without taking over Enter.
return {
  'RRethy/nvim-treesitter-endwise',
  lazy = false,
  dependencies = { 'nvim-treesitter/nvim-treesitter' },
}
